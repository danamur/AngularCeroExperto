import { FiltroTareasPipe } from './filtro-tareas.pipe';

describe('FiltroTareasPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTareasPipe();
    expect(pipe).toBeTruthy();
  });
});
